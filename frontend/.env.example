# No env vars needed for frontend in dev mode
# Vite proxies /api -> http://localhost:5000 automatically via vite.config.js
#
# For production builds, set the API URL:
# VITE_API_URL=https://your-api-domain.com/api
